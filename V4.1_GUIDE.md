# Codex Engineering System V4.1 Guide

V4.1 keeps the V4 compact architecture: **18 Core Skills active by default**, with **196 total specialist Skills** in the library.

## New Skills

- `vb-dotnet-engineering`
- `winforms-production`
- `wpf-production`
- `legacy-dotnet-modernization`

## New Packs

- `dotnet-vb`
- `dotnet-desktop`

## New Profiles

- `vb-dotnet-desktop`
- `dotnet-desktop`
- `web-dotnet-vb`

## VB.NET Profile

```powershell
& "$HOME\.codex-engineering-system\v4.1\codex-pack.ps1" profile vb-dotnet-desktop
```

## Auto-detect a VB.NET project

```powershell
& "$HOME\.codex-engineering-system\v4.1\codex-pack.ps1" auto -ProjectPath "C:\path\to\VBProject"
```

The detector recognizes `.vbproj` and checks for WinForms/WPF markers.

## Install/update

```powershell
Set-ExecutionPolicy -Scope Process Bypass
.\install-update.ps1 -WhatIf
.\install-update.ps1
.\verify-install.ps1
.\verify-active-budget.ps1
```

Expected: library 196, Core 18.
