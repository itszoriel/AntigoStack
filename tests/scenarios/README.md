# Evaluation scenarios

These Markdown files are specification-based evaluation scenarios. They define desired behavior and review criteria, but they are not executable tests and do not prove that a model will behave as specified.

To turn them into model evaluations, add a documented runner, fixed inputs, evaluator rubric, model/version metadata, repeat strategy, and retained results. Keep model credentials and raw sensitive data out of the repository.
