# Evaluation Run Records

Store local run records here while testing. Commit only results you intentionally want to publish as evidence.

Do not commit secrets, private repository content, hidden reasoning, credentials, or raw transcripts containing sensitive data.

A result record may reference an external/raw transcript without embedding it in the repository.
