# Codex Engineering System V5 — Professional Intelligence

A compact Codex agent/Skill system with 18 always-active Core Skills and a 197-Skill specialist library loaded on demand.

V5 focuses on professional reasoning: truth over agreement, evidence, version-aware decisions, proportional architecture, explicit verification, detailed manual handoffs, deployed-runtime checks, and cleaner GitHub/release behavior.

Start with `V5_GUIDE.md`, run `verify-package.ps1`, then preview/install with `install-update.ps1`.

The package also includes `V5_SKILL_RESEARCH_MATRIX.md` so the source families behind all 197 specialist Skills can be audited.

See `STATIC_QA_REPORT.md` for the exact package checks performed and the PowerShell runtime verification boundary.
